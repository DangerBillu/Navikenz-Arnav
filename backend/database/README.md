# Database
This folder has SQLAlchemy database setup and schema maintenance.

## Files
- `connection.py` defines the engine, session, database structure, schema compatibility helpers, and guest chat count.