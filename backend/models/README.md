# Models
This folder contains SQLAlchemy ORM models. Models describe database tables and relationships.

## Files
- `user.py` defines authenticated and guest users.
- `chat_session.py` defines chat sessions and their per-user identification numbers.
- `message.py` stores user prompts and assistant replies.
