# Schemas
This folder contains Pydantic models used as API request and response contracts.

## Files
`chat.py` defines validation schemes for chat creation, message sent, message response and dashboard response.
