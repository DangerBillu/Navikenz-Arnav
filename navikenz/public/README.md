# Public Assets

This folder contains static files served directly by Vite.

## Files

- `favicon.svg` is the browser favicon.
- `icons.svg` stores static icon artwork.

## Guidelines

- Use this folder for assets that need stable public URLs.
- Use `src/assets` for assets imported by React components or bundled by Vite.
