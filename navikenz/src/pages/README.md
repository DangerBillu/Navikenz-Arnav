# Pages
This folder contains the frontend pages like the signup page and the dashboard. 

## Files
- `SignInPage.jsx` renders login, signup and continue as guest entry points.
- `DashboardPage.jsx` renders chat history, active messages, sending/deleting chats and the guest chat limit modal.
