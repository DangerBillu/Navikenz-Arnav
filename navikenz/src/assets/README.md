# Assets

This folder contains frontend assets imported by React or bundled by Vite.

## Files

- `hero.png` is used by the sign-in experience.
- `react.svg` and `vite.svg` are default starter assets and can be removed when no longer used.

## Guidelines

- Keep component-imported images here.
- Put directly served public files in `navikenz/public`.
