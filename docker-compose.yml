version: "3.9"

services:
  postgres:
    image: postgres:17

    container_name: postgres_db

    restart: always

    environment:
      POSTGRES_USER: arnav
      POSTGRES_PASSWORD: password12
      POSTGRES_DB: mydatabase

    ports:
      - "5432:5432"
